{
  "name": "ryan",
  "version": "1.0.0",
  "description": "> My personal site, built with Elm!",
  "main": "index.js",
  "scripts": {
    "start": "serve public",
    "dev": "npm start & npm run build:watch",
    "build": "elm make src/Main.elm --output public/elm.js || true",
    "build:watch": "npm run build && chokidar './src' -c 'npm run build'"
  },
  "author": "Ryan Haskell-Glatz",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "^1.2.1",
    "elm": "^0.19.0-bugfix2",
    "serve": "^10.1.1"
  }
}
